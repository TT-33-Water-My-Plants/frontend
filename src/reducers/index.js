

export const initialState = {
    isLoading: false,
    plants: null,
    error: 'hello'
}

export const reducer = (state = initialState, action) => {
    switch(action.type){
        
         default:
             return state
    }
}